# 悬浮球余量口径修复

- 日期：2026-07-07
- 类型：fix
- 关联文件：src/shared/format.ts、scripts/verify-today-percent.ts

## 变更摘要

- 折叠态悬浮球余量优先使用 Dashboard `metrics.totalUsedPercent` 反算，确保与展开面板「总消耗」互补
- 保留 `totalRemaining / totalLimit` 作为没有 Dashboard 总消耗时的兜底
- 补充回归场景，覆盖 `totalPercentUsed=5.35` 时悬浮球余量应为 `94.65%`（展示为 `95%`）

## 影响范围

- 修复 Cookie 数据源下悬浮球余量与展开面板总消耗口径不一致的问题
- 不影响今日 API / 今日 Auto + Composer 的百分比推导逻辑

## 验证

- 临时编译 `scripts/verify-today-percent.ts` 及依赖到系统临时目录后用 Node 执行，全部场景通过
- `npm run typecheck`
