# Qoder 项目级变更归档规则

- 日期：2026-07-25
- 类型：docs
- 关联文件：.qoder/rules/change-archive.md

## 变更摘要

- 参考 `.cursor/rules/change-archive.mdc` 创建 Qoder 项目级规则 `.qoder/rules/change-archive.md`（`trigger: always_on`，对应源规则的 `alwaysApply: true`）
- 规则内容与 Cursor 版保持一致：归档范围、步骤、可合并归档、示例文件名
- 额外内嵌归档文档模板（日期/类型/关联文件/变更摘要/影响范围/验证），与 `docs/changes/` 现有条目格式对齐

## 影响范围

- 仅影响 Qoder IDE 下的 Agent 行为约束，不涉及代码与构建

## 验证

- 人工核对规则内容与 `.cursor/rules/change-archive.mdc` 及现有归档条目格式一致
