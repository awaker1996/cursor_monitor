# 语言包裁剪与文档同步

- 日期：2026-07-06
- 类型：build, docs
- 关联文件：package.json, electron-builder.yml, README.md, requirements.md, docs/changes/, .cursor/rules/change-archive.mdc

## 变更摘要

### 体积优化（第二阶段）

- 添加 `electronLanguages: [en-US, zh-CN]`，裁剪 Chromium 语言包
- 添加 `compression: maximum`（electron-builder 根级配置）
- locales 从 ~36 MB 降至 ~0.84 MB
- 便携版 EXE 从 ~73.5 MB 降至 ~67.2 MB

### 文档同步

- 更新 `README.md`：构建链、FR-07~09、Dashboard metrics、贴边收起、自定义图标、快照缓存、配置项、IPC、打包体积说明
- 更新 `requirements.md`：同步功能需求、Cookie 映射（usage-summary）、metrics、验收标准

### 变更归档机制

- 新建 `docs/changes/` 目录与索引
- 新建 `.cursor/rules/change-archive.mdc` 强制代码变更归档

## 影响范围

- 打包产物体积显著减小（locale 裁剪为主要收益）
- 文档与当前业务代码对齐
- 后续代码改动需同步归档至 `docs/changes/`

## 验证

- `npm run dist` 成功，便携版 ~67.2 MB、安装包 ~67.4 MB
- `win-unpacked/locales/` ~0.84 MB（仅 en-US + zh-CN）
- 应用冒烟测试启动正常
- README / requirements 已反映当前功能
