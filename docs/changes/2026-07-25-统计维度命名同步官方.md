# 统计维度命名同步官方（Cursor Models / Other Models）

- 日期：2026-07-25
- 类型：refactor, docs
- 关联文件：src/shared/format.ts、src/shared/types.ts、src/core/normalizer.ts、src/renderer/components/IncludedUsageTable.tsx、src/renderer/components/TestConnectionResultPanel.tsx、README.md、requirements.md

## 变更摘要

- 同步 Cursor 官方最新命名：`First-party models` / `FP` 统一改为 `Cursor Models`，`API` 统一改为 `Other Models`
- `format.ts` 中 `FIRST_PARTY_MODELS_LABEL` 常量重命名为 `CURSOR_MODELS_LABEL`，新增 `OTHER_MODELS_LABEL`，metric 卡片与托盘 tooltip 文案统一走常量
- Included Usage 分区标签与徽章（原 `API` / `FP`）改为 `Other` / `Cursor` 徽章 + 新分区名
- 设置页测试连接结果的配额行标签同步（`API 配额` → `Other Models 配额` 等）
- 内部字段与 CSS accent（`api` / `firstParty` / `auto`）保持不变，仅调整展示文案
- 展示顺序调换：Cursor Models 优先于 Other Models（metric 卡片、托盘 tooltip、Included Usage 分区、测试连接配额行）
- README 与 requirements 文档中相关表述同步

## 影响范围

- 悬浮球概览四张 metric 卡片标签与顺序
- 托盘悬停 tooltip 分项名称与顺序
- Included Usage 明细分区标题、徽章与分区顺序
- 设置页测试连接结果面板配额行

## 验证

- npm run typecheck：通过
- npx tsx scripts/verify-included-usage.ts：全部通过
- npx tsx scripts/verify-today-percent.ts：全部通过
