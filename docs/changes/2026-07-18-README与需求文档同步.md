# README 与需求文档同步

- 日期：2026-07-18
- 类型：docs
- 关联文件：README.md、requirements.md

## 变更摘要

- 同步刷新间隔下限为 30 秒（原 README 误写 10 秒）
- 补充悬浮球概览/明细分段、折叠余量、展开动画与窗体尺寸等业务设计
- 补充托盘悬停多行概览用量、连接测试结构化详情、刷新加载反馈等已实现能力
- 更新模块/目录结构、验收清单与 v1 功能范围

## 影响范围

- 项目参考文档与需求追溯文档，无运行时行为变更

## 验证

- 对照 `src/shared/format.ts`（`formatTrayTooltip`）、`src/shared/types.ts`（`REFRESH_INTERVAL_MIN = 30`）、`src/renderer/App.tsx` 确认描述一致
