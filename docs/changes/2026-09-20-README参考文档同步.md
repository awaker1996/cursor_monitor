# README 参考文档同步（设置窗口三分区与订阅流水现状）

## 日期
2026-09-20

## 类型
docs

## 关联文件
- `README.md` — 项目参考文档全篇同步（无代码改动）

## 变更摘要

### 第 1 章 项目概述
- 开头简介补充「设置窗口内按 订阅 / 流水 / 其他 三个分区承载」；「背景与目标」补订阅额度查询
- 「范围内」由 10 条重写为 12 条：补设置窗口三分区 Tab、订阅额度与用量查询（Cursor / Command Code / DeepSeek）、流水按平台查询、凭据范围扩展到订阅 API Key / 用量 Token、订阅缓存；托盘右键项修正为「设置 / 复位 / 退出」
- 「范围外」补「历史趋势图表（首版不做）」

### 第 2 章 快速开始
- 开发模式标注 dev server 固定 `http://localhost:5180`，端口被占用会直接报错退出

### 第 3 章 功能需求
- FR-06：托盘右键菜单由「查看流水 / 应用设置 / 重置悬浮 / 退出」更正为「设置 / 复位 / 退出」，补左键弹出同一菜单、tooltip 模型名对齐与末行合并、托盘图标来源
- FR-10：「用量流水窗口」改写为「用量流水」——Tab 化、进入不自动加载、平台筛选（Cursor / DeepSeek）、每页 10/20/50/100 默认 20、模型统计面板
- 新增 FR-11 订阅额度与用量查询（三个提供方的数据形态、刷新策略、缓存与提示分级）
- 新增 FR-12 设置窗口结构（尺寸、三分区、其他分区四张卡片与核心功能卡的 README 派生机制）

### 第 4 章 架构设计
- 架构图补 `SubscriptionManager` / `SubscriptionCache` / 三个订阅 Provider 与设置窗口到 ProviderManager、SubscriptionManager 的连线，移除已无引用的 `FlowWindow`
- 模块职责表补 20 行（订阅链路、`devServer.ts`、`snapshotMerge.ts`、`usageFlowPagination.ts`、TabBar / SettingsTabContent / ProjectFeaturesCard / ProviderSwitcher / SubscriptionPanels / SubscriptionUtils / UsageFlowModelStats 等），并注明 `electron/windows/flow.ts`、`subscriptions.ts` 当前无引用

### 第 5 章 目录结构
- 按实际文件重写：新增 `src/core/subscriptions/*`（6 个文件）、`snapshotMerge.ts`、`shared/subscriptionTypes.ts`、`shared/usageFlowPagination.ts`、`shared/devServer.ts`、`subscriptions-main.tsx` 与新增组件；删去已不存在的 `IncludedUsageModelName.tsx`；标注 `flow.html`、`subscriptions.html` 当前无引用

### 第 8 章 安全设计
- 补订阅凭据存储（`CredentialVault` 按账户名区分）与 Command Code Key 三级解析（手动 → 环境变量 → `~/.commandcode/auth.json`）

### 第 9 章 界面与交互
- 「设置页」重写为「设置窗口（三分区）」，分别描述订阅 / 流水 / 其他的内容
- 托盘悬停示例改为 5 行对齐格式（模型名补齐、末行「来源 + 更新」）；右键菜单更正为设置 / 复位 / 退出

### 第 12～14 章
- 验收清单：更正托盘菜单与流水分页描述，新增三分区、订阅提供方、缓存回显、失败保留、流水手动刷新、其他分区同步刷新、核心功能卡一致性等条目
- 维护与扩展：「接口变更时改哪里」拆出订阅平台章节；「新增 Provider」拆为主链路与订阅提供方；IPC 一览按实际通道重写（补 `subscription-*`、`open-subscriptions`、`get-cursor-in-window`、`set-orb-mode-async`、`set-ignore-mouse-events` 等，并标注 `set-expanded` 为空实现）；常见问题补 6 条
- 风险补 Command Code alpha 接口无公开承诺、DeepSeek 用量接口依赖浏览器凭据、默认图标 `build/icon.png` 与打包清单 `build/icon.ico` 文件名不一致的隐患；v1 版本内容更新；文末版本号 v1.3 → v1.4

## 影响范围
- 仅文档改动，无源码 / 配置变更
- 设置窗口「其他」分区的「核心功能」卡内容派生自 README（第 1 章开头简介 + 「范围内」清单，经 `?raw` 导入），因此该卡文案随本次同步更新，属预期联动
- README 已成为一处 UI 内容来源：后续修改开头简介或「范围内」清单会直接反映到设置页

## 验证
- `git status` 复核：本次改动文件仅 `README.md`
- 用临时壳页（注入 `electronAPI` mock）+ headless Chrome 按设置窗口内容区尺寸（1118×689）截图：核心功能卡已显示新简介与「范围内」12 条清单，超出高度部分在卡内滚动，卡片布局未受影响
- 事实抽核（对照源码而非凭记忆）：托盘菜单项与 tooltip 格式（`electron/tray.ts`、`src/shared/format.ts`）、IPC 通道清单（`electron/main.ts`、`electron/preload.ts`）、窗口尺寸与标题覆盖行为（`electron/windows/*.ts`、各 HTML 的 `<title>`）、流水分页默认值与平台选项（`src/shared/usageFlowPagination.ts`、`src/renderer/pages/FlowPage.tsx`）、订阅提供方顺序与缓存策略（`src/core/subscriptions/*`）
- 待用户执行：通读一遍 README，确认有无与预期不符的表述
