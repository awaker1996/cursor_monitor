# 流水窗布局与日期格式

- 日期：2026-07-24
- 类型：fix
- 关联文件：electron/windows/flow.ts、src/shared/format.ts、src/renderer/pages/FlowPage.tsx、src/renderer/components/UsageFlowModelStats.tsx、src/renderer/components/UsageFlowTable.tsx、src/renderer/styles.css

## 变更摘要

- 模型统计区去掉重复的周期副标题（筛选器已展示范围）
- 流水窗默认 1120×720，表格区 flex 布局避免末行被裁切
- 流水 Date 列格式为东八区 `YYYY/MM/DD HH:mm`（如 2026/07/24 14:38），表头为 Date
- 表格区改用 grid 固定分页行，内层 padding 避免末行被滚动区域裁切

## 影响范围

- 用量流水窗口尺寸与表格展示

## 验证

- 打开流水窗，末行完整可见；日期为中文格式
